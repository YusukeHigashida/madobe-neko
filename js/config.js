"use strict";
/* ============================================================
   設定
   ============================================================ */

// 起動時の状態
// followRealTime: true なら東京の現在時刻に合わせ、そのまま実時間で進む。
//                 再生ボタンやスライダーを触るとタイムラプスに切り替わる。
// behavior: false にすると猫は座ったまま動かなくなる（プレゼンで固定したいとき用）
const START = { hour: 12.0, weather: 'clear', playing: false, behavior: true, followRealTime: true };

// 1日1周にかける秒数（体感 30〜60秒）
const DAY_CYCLE_SEC = 60;

// 素材の置き場所。assets/ → index.html と同じ階層 の順に探す。
// （後者は、画像を直置きしていた昔の配置でも動くようにするための保険）
const ASSET_BASES = ['assets/', ''];
const ASSETS = {
  cat:     'cat2.png',
  poses:   'cat-poses.png',    // 伸び / 香箱 / 寝る（1枚に3ポーズ）
  walk:    'cat-walk.png',     // 歩行4コマ
  day:     'bg-day-city2.png',
  // ↓ 将来この名前で画像を置けば、その時間帯だけ自動的に本物の絵に差し替わる
  morning: 'bg-morning.png',
  sunset:  'bg-sunset.png',
  night:   'bg-night.png',
};

/* ------------------------------------------------------------
   猫の姿勢（スプライト）
   cell : シート上の切り出し矩形（ピクセル）
   ground: そのコマの足元の y（シート座標）。ここが窓台の線に来るように置く
   widthPct: ステージ幅に対する表示幅。猫の大きさが姿勢ごとに変わらないよう調整済み
   ------------------------------------------------------------ */
const CAT_GROUND_Y = 86;         // 窓台の上で猫が接地する高さ（ステージ高さの%）

// clip は「シート上で、この左右の範囲だけを見せる」指定（ピクセル）。
// セルが隣のコマにはみ出すので、隣の猫が端に写り込むのを防ぐ。
const POSES = {
  sit:     { sheet:'cat',   cell:[   0,   0, 1536, 1024], ground: 912, widthPct: 42.0 },
  stretch: { sheet:'poses', cell:[  20,   0,  800,  667], ground: 613, widthPct: 40.3, clip:[   0,  802] },
  loaf:    { sheet:'poses', cell:[ 790,   0,  600,  667], ground: 628, widthPct: 32.5, clip:[ 802, 1400] },
  sleep:   { sheet:'poses', cell:[1405,   0,  590,  667], ground: 623, widthPct: 33.5, clip:[1401, 2000] },
  // 歩行は「目の位置」を基準に4コマを切り出す（頭がブレないように）
  walk:    { sheet:'walk',  ground: 517, widthPct: 38.1,
             frames: [[-17,68],[478,69],[946,68],[1454,69]], frameW: 560, frameH: 470,
             frameClip: [[0,521],[517,981],[977,1486],[1482,1966]],
             fps: 4.6 },
};

/* ------------------------------------------------------------
   BGM（すべてコードで生成。音源ファイル不要・ライセンス問題なし）
   時間帯ごとに「調・音域・明るさ・音数」を変え、天気でさらに味付けする。
   曲を切り替えるのではなくパラメータを変えるので、継ぎ目が出ない。
   ------------------------------------------------------------ */
const BGM_ENABLED = true;

const BGM_MOODS = [
  // from: この時刻から / root: 主音(MIDI) / prog: コード進行(主音からの半音)
  // scale: 旋律に使う音 / cutoff: 明るさ / noteEvery: 音の間隔(秒) / pad: 和音の音量
  { from:  4.6, name:'あさ',  root: 57, prog:[0, 5, 7, 5],  scale:[0,2,4,7,9],  cutoff: 2500, noteEvery: 2.6, bellOct: 2, pad: .075, major: true  },
  { from:  8.4, name:'ひる',  root: 60, prog:[0, 5, 9, 7],  scale:[0,2,4,7,9],  cutoff: 3100, noteEvery: 2.2, bellOct: 2, pad: .070, major: true  },
  { from: 16.4, name:'ゆうがた', root: 53, prog:[0, 7, 5, 3], scale:[0,2,5,7,9], cutoff: 1700, noteEvery: 3.2, bellOct: 1, pad: .088, major: true  },
  { from: 19.6, name:'よる',  root: 45, prog:[0, 3, 7, 5],  scale:[0,3,5,7,10], cutoff:  950, noteEvery: 4.4, bellOct: 1, pad: .098, major: false },
];

// 歩ける範囲（猫の中心・ステージ幅の%）。鉢植えの手前を通れる程度に取る
const WALK_RANGE = [36, 70];
const WALK_SPEED = 6.4;          // %/秒

// 実際の天気との同期（Open-Meteo / APIキー不要・無料）
// 別の街にしたいときは name / lat / lon を変えるだけ。
const LIVE_WEATHER = {
  enabled: true,
  name: '東京',
  tz:  'Asia/Tokyo',   // 時刻の同期にも使う
  lat: 35.6895,
  lon: 139.6917,
  refreshMinutes: 10,   // 同期中は定期的に取り直す
  timeoutMs: 6000,      // 社内ネットワーク等で詰まったときに待ち続けない
};

// 窓ガラスの区画（ステージに対する % ）。雨・雪・星・雲・空の色はこの中だけに描く。
// ★ 区画の境界は必ず木枠（外枠・中央の縦框・窓台）の位置とぴったり合わせる。
//    ずれると、天気のかすみが「四角い板」として見えてしまう。
const PANES = [
  { x: 13.9, y: 3.4, w: 36.8, h: 75.6 },   // 左ガラス
  { x: 56.0, y: 3.4, w: 31.9, h: 75.6 },   // 右ガラス
];

/* ------------------------------------------------------------
   時間帯カラーテーブル
   mul   : 乗算で重ねる色（暗さ・色被り）
   scr   : スクリーンで重ねる色（光・空気感）
   bri/sat: 画面全体のフィルタ
   beam  : 窓から差す光の強さ / 角度
   ------------------------------------------------------------ */
const TIMELINE = [
  { h: 0,    mul:[ 34, 44, 92,.54], scr:[ 62, 84,164,.10], bri:.72, sat:.68, beam:0,   beamX:  0 },
  { h: 4.2,  mul:[ 40, 50,100,.51], scr:[ 76, 90,170,.10], bri:.75, sat:.72, beam:0,   beamX:-14 },
  { h: 5.4,  mul:[ 92, 68,110,.44], scr:[255,150,118,.14], bri:.78, sat:.84, beam:.20, beamX:-24 },
  { h: 6.6,  mul:[255,186,150,.28], scr:[255,172,112,.18], bri:.94, sat:1.00, beam:.55, beamX:-20 },
  { h: 8.5,  mul:[255,222,188,.12], scr:[255,216,158,.09], bri:1.02,sat:1.04, beam:.62, beamX:-12 },
  { h: 12,   mul:[255,255,255,.00], scr:[255,248,224,.03], bri:1.06,sat:1.06, beam:.42, beamX:  0 },
  { h: 15,   mul:[255,232,196,.08], scr:[255,206,140,.07], bri:1.03,sat:1.06, beam:.52, beamX: 10 },
  { h: 17.4, mul:[255,170,110,.30], scr:[255,142, 66,.20], bri:.95, sat:1.12, beam:.72, beamX: 20 },
  { h: 18.8, mul:[168, 88,116,.44], scr:[255, 96, 62,.16], bri:.80, sat:.98, beam:.34, beamX: 27 },
  { h: 20.2, mul:[ 78, 64,122,.50], scr:[110, 92,168,.12], bri:.78, sat:.80, beam:.06, beamX: 20 },
  { h: 22,   mul:[ 40, 50,100,.53], scr:[ 68, 88,166,.10], bri:.73, sat:.70, beam:0,   beamX:  6 },
  { h: 24,   mul:[ 34, 44, 92,.54], scr:[ 62, 84,164,.10], bri:.72, sat:.68, beam:0,   beamX:  0 },
];
